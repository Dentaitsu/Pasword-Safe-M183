spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/passwordmanager}
    username: ${DB_USER:admin}
    password: ${DB_PASSWORD:secret}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080